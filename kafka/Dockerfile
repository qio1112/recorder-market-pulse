# Simple Kafka Dockerfile for single-node setup
FROM bitnami/kafka:3.6.1

# No extra configuration needed for single-node KRaft mode.
# Configuration is handled via environment variables or docker-compose. 